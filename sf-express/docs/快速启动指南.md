# 快速启动指南

## 🚀 一键启动（已配置完成）

服务器已经配置完成，直接启动即可：

```bash
python run.py
```

然后访问：http://localhost:8000/admin/login.html

**登录信息**：
- 用户名：`admin`
- 密码：`admin123`

---

## 📦 首次安装配置（仅供参考）

如果需要在新环境中重新配置，按以下步骤操作：

### 1. 安装Python依赖
```bash
pip install -r requirements.txt
```

### 2. 配置环境变量
复制 `.env.example` 为 `.env`，然后修改数据库密码：
```
DATABASE_URL=mysql+pymysql://root:你的密码@localhost:3306/express_tracking
```

### 3. 启动MySQL服务
```bash
# Windows
net start MySQL80

# 或在服务管理器中启动
services.msc
```

### 4. 创建数据库和表
```bash
python setup_database.py
```

### 5. 创建管理员用户
```bash
python create_admin_user.py admin admin123
```

### 6. 启动服务器
```bash
python run.py
```

---

## 🧪 测试服务器

### 快速测试
```bash
python test_server_running.py
```

### 完整测试
```bash
python quick_test.bat
```

---

## 🔧 常见问题

### Q1: 服务器无法启动
**A**: 运行诊断脚本
```bash
python capture_error.py
```

### Q2: MySQL连接失败
**A**: 检查MySQL服务状态
```bash
python check_mysql_service.py
```

### Q3: 端口8000被占用
**A**: 查看占用端口的进程
```bash
netstat -ano | findstr :8000
```

### Q4: 忘记管理员密码
**A**: 重新创建管理员用户
```bash
# 先删除旧用户（在MySQL中）
mysql -u root -p
USE express_tracking;
DELETE FROM admin_users WHERE username='admin';
EXIT;

# 创建新用户
python create_admin_user.py admin 新密码
```

---

## 📚 相关文档

- **详细测试指南**: `平台测试指南.md`
- **配置完成说明**: `服务器配置完成.md`
- **启动成功总结**: `启动成功总结.md`
- **CSV上传功能**: `.kiro/specs/csv-file-upload/`

---

## 🎯 主要功能

1. **快递查询** - 输入单号查询物流信息
2. **理货单管理** - 上传CSV文件批量管理货物
3. **数据统计** - 查看货物统计信息
4. **用户管理** - 管理后台用户

---

## 📞 技术支持

如遇到问题，请提供以下信息：
1. 错误截图
2. 运行 `python capture_error.py` 的输出
3. 运行 `python check_mysql_service.py` 的输出

---

**当前状态**: ✅ 服务器运行正常  
**服务器地址**: http://localhost:8000  
**管理后台**: http://localhost:8000/admin/login.html
